# Backend API URL.
# Local dev: backend runs at localhost:8000 (uv run uvicorn or docker compose).
# Production (Cloudflare Tunnel): use the public domain that fronts the API.
NEXT_PUBLIC_API_URL=http://localhost:8000
